/** @type {import('tailwindcss').Config} */
// Tailwind v4 uses CSS-first configuration via @theme directive
// This config file is kept for content paths and legacy compatibility
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
}
